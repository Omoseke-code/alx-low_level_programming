#include <stdio.h>
#include <stdlib.h>

/**
 * main - entry point
 * @argc: lists the number of programs
 * @argv: holds the value
 * Return: 0 on success
 */
int main(int argc, char *argv[])
{
	(void) argc;

	printf("%s\n", argv[0]);
	return (0);
}
